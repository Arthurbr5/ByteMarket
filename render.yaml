services:
  # Backend API (Node.js + Mercado Pago)
  - type: web
    name: bytemarket-api
    runtime: node
    buildCommand: npm install
    startCommand: node server-mercadopago.js
    envVars:
      - key: NODE_ENV
        value: production
